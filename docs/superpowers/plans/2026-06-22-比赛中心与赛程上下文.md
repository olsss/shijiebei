# 比赛中心与赛程上下文实施计划

## 约束

- Java 17 / Spring Boot 3.3.6。
- 复用 V3/V4 表，不新增数据库迁移。
- 所有接口需认证。
- 不修改 `CLAUDE.md`、`skill/`。
- 先写测试，再实现。

## 步骤

1. OpenSpec 与设计
   - 新建 change：`比赛中心与赛程上下文`。
   - 写 proposal/design/tasks/spec。
   - 执行 `openspec validate 比赛中心与赛程上下文 --strict`。
   - 提交设计与 OpenSpec 文档。

2. 后端 TDD
   - 新增 `MatchCenterControllerTest`，覆盖认证、列表、详情、子资源和 404。
   - 运行目标测试，确认因缺少实现失败。
   - 新增 `MatchCenterDtos`、`MatchCenterQueryService`、`MatchCenterController`。
   - 运行目标测试直至通过。

3. 前端 TDD
   - 新增 `matches-api.test.ts` 并更新 `router.test.ts`。
   - 运行前端测试，确认因缺少实现/路由失败。
   - 新增 `matches.ts`、`MatchCenterView.vue`，更新路由与 Dashboard。
   - 运行前端测试直至通过。

4. 完整验证
   - `openspec validate 比赛中心与赛程上下文 --strict`
   - `mvn -f server/pom.xml test`
   - `npm run test:run`
   - `npm run build`
   - `git diff --check`
   - protected diff。

5. 代码审查
   - 启动子代理审查后端/前端/OpenSpec/规则边界。
   - 处理有效反馈。
   - 生成 `docs/java-system/reviews/比赛中心与赛程上下文-review.md`。

6. 合并
   - 最终验证通过后提交实现。
   - 合并 `feature/match-center` 到 `main`。
   - 清理 worktree。
