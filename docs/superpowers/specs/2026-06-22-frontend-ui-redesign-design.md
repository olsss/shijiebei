# 前端整站重设计与 H5 适配设计

日期：2026-06-22  
状态：已获用户确认，待实现计划  
范围：前端整体重构；允许为体验与权限边界调整后端接口/安全配置。

## 1. 背景与目标

当前前端基于 Vue 3 + Element Plus，页面已覆盖 JSON 审核、比赛中心、赔率中心、舆情中心、分析下注复盘、赛前作战室、球队画像、球员画像、系统设置等模块，但整体仍偏“功能堆叠型后台”。本次重设计目标是把系统重塑为“世界杯竞彩情报与决策指挥舱”：既有赛事沉浸感，又保留下注分析所需的数据密度、证据链和可读性。

核心目标：

- 采用赛事沉浸 / 品牌化视觉方向，而不是普通后台模板。
- 重新定义模块结构，但不丢失现有功能。
- 同时服务 PC 与手机 H5，不只做桌面端。
- 未登录用户可浏览只读数据；管理员登录后才能执行审核、批准、入库、保存、修改、删除等写操作。
- 前端权限只是体验层，后端必须对写接口强制鉴权。
- 保持项目下注规则：官方核对、多源证据、赔率价值、CLV、复盘沉淀等内容在界面层被明确呈现。

## 2. 已确认决策

### 2.1 视觉方向

选择 **C：赛事沉浸 / 品牌化**。

设计约束：

- 首页和导航可体现世界杯赛事氛围。
- 业务数据页必须克制，避免过度 HUD、细线、发光、背景图干扰阅读。
- 图标必须使用一致的 SVG 图标体系，不使用 emoji 作为结构性图标。
- 所有可交互控件触控区域不小于 44px。

### 2.2 改造范围

选择 **前端整体重构**，并允许改后端：

- 可抽公共布局和设计系统组件。
- 可重写多数页面结构与样式。
- 可调整后端安全规则、只读聚合接口、DTO。
- 不删除现有业务能力。

### 2.3 信息架构

选择 **A：五阶段决策流**。

一级区：

1. 赛事总览
2. 赛前作战
3. 证据中心
4. 决策与复盘
5. 管理后台

### 2.4 默认首页

未登录打开系统时进入 **赛事沉浸总览首页**，展示今日比赛、关键风险、完整性摘要、只读入口与管理员入口。

### 2.5 权限边界

- 未登录：可访问只读页面和查询型接口。
- 管理员：可执行 JSON 审核、扫描、批准、驳回、入库、保存、修改、删除、采集项批准等写动作。
- JSON 审核中心整体作为管理后台功能，不向未登录用户开放，因为它可能暴露原始归档内容和入库操作上下文。

## 3. UI/UX 设计系统

### 3.1 产品类型与风格

基于 ui-ux-pro-max 设计系统查询，本项目匹配：

- Product pattern：Real-Time / Operations Landing
- Style：Data-Dense Dashboard + Real-Time Monitoring + Sports Event Immersive
- 设计关键词：实时态势、赛事氛围、数据密集、证据完整性、风险提醒、决策路径。

### 3.2 色彩 Tokens

建议在 `client/src/styles/main.css` 或拆分后的 token 文件中定义语义变量：

```css
:root {
  --wc-bg: #07111f;
  --wc-bg-elevated: #0b1730;
  --wc-surface: #101b2c;
  --wc-surface-2: #16253a;
  --wc-glass: rgba(15, 23, 42, 0.72);
  --wc-border: rgba(148, 163, 184, 0.22);

  --wc-text: #eef6ff;
  --wc-text-muted: #98abc7;
  --wc-text-subtle: #64748b;

  --wc-primary: #3b82f6;
  --wc-primary-deep: #1e40af;
  --wc-accent: #d97706;
  --wc-success: #16a34a;
  --wc-warning: #f59e0b;
  --wc-danger: #dc2626;
  --wc-cyan: #22d3ee;

  --wc-radius-sm: 10px;
  --wc-radius-md: 14px;
  --wc-radius-lg: 22px;
  --wc-radius-xl: 30px;
  --wc-focus-ring: 0 0 0 3px rgba(217, 119, 6, 0.36);
}
```

说明：

- 深色为默认主题。
- 琥珀色只用于关键行动、赛事氛围和重要提示，不滥用于正文。
- 绿/黄/红只表达状态，并配合文字，不仅靠颜色传达意义。

### 3.3 字体策略

- 大标题：运动感 Condensed 字体风格，例如 `Barlow Condensed`，用于首页英雄区和模块标题。
- 正文与表格：`Barlow` / `Inter` / `Microsoft YaHei` / `PingFang SC`，保证中文阅读稳定。
- 数字与赔率：`Fira Code` 或等宽数字设置，用于赔率、金额、CLV、编号、时间。
- 字体加载必须使用 `font-display: swap`；如果不引入在线字体，则使用系统字体降级。

### 3.4 布局和响应式

采用移动优先 + 桌面增强：

- 375px：手机 H5 基准。
- 768px：平板。
- 1024px：小桌面。
- 1440px：标准桌面。

桌面：

- 左侧固定五阶段导航。
- 顶部状态栏显示北京时间、只读/管理员状态、登录入口。
- 内容区最大宽度按页面类型控制，数据页允许更宽。

手机 H5：

- 顶部显示赛事标题、当前状态、登录入口。
- 底部使用 5 项主导航：总览、赛前、证据、决策、更多。
- 管理后台放入“更多/管理员入口”，避免底部导航拥挤。
- 首页 KPI 从 4 列降为 1-2 列。
- 五阶段入口从横向流改为纵向卡片。
- 筛选器使用抽屉或折叠面板，不占满首屏。
- 固定底部栏使用 `padding-bottom: env(safe-area-inset-bottom)`。
- 禁止整页横向滚动；大表格优先转卡片列表，确需表格时只让表格容器横向滚动。

### 3.5 交互与可访问性

- 所有按钮、导航、图标按钮触控区域 ≥44px。
- hover、active、focus、disabled 状态必须明确。
- 键盘 Tab 顺序与视觉顺序一致。
- 提供跳到主内容的 skip link。
- 路由切换后主内容区域可被辅助技术识别。
- 加载超过 300ms 显示 skeleton 或 loading。
- 动画控制在 150-300ms，并支持 `prefers-reduced-motion`。
- 文字对比度：正文至少 4.5:1，大号文字至少 3:1。

## 4. 信息架构与路由

### 4.1 新路由

建议路由：

| 新路由 | 名称 | 访问模式 | 说明 |
|---|---|---|---|
| `/` | 赛事总览 | 公开只读 | 未登录默认首页 |
| `/workbench` | 赛前作战 | 公开只读，写动作需登录 | 聚合赛前分析上下文 |
| `/evidence/matches` | 比赛与赛程 | 公开只读 | 原比赛中心 |
| `/evidence/odds` | 赔率与盘口 | 公开只读 | 原赔率中心 |
| `/evidence/sentiment` | 舆情与外部因素 | 公开只读 | 原舆情中心 |
| `/evidence/teams` | 球队画像 | 公开只读，采集项审批需登录 | 原球队画像中心 |
| `/evidence/players` | 球员画像 | 公开只读，采集项审批需登录 | 原球员画像中心 |
| `/decisions` | 决策与复盘 | 公开只读，编辑/结算需登录 | 原分析下注复盘中心 |
| `/admin/import-review` | JSON 审核中心 | 管理员 | 扫描、审核、批准、驳回、入库 |
| `/admin/settings` | 系统设置 | 管理员 | 系统配置 |
| `/login` | 登录 | 公开 | 登录后回跳 |

### 4.2 旧路由兼容

保留重定向：

| 旧路由 | 新路由 |
|---|---|
| `/matches` | `/evidence/matches` |
| `/odds` | `/evidence/odds` |
| `/sentiment` | `/evidence/sentiment` |
| `/analysis-review` | `/decisions` |
| `/prematch-workbench` | `/workbench` |
| `/profiles/teams` | `/evidence/teams` |
| `/profiles/players` | `/evidence/players` |
| `/import-review` | `/admin/import-review` |
| `/settings` | `/admin/settings` |

## 5. 页面设计

### 5.1 赛事总览

目的：公开只读入口，一屏进入状态。

内容：

- 今日/近期比赛英雄区。
- 官方核对、赔率时效、证据完整性、风险提醒 KPI。
- 高风险比赛队列。
- 五阶段入口卡片。
- 管理员待办摘要，但未登录仅显示数量或提示，不暴露原始 JSON 内容。
- 快捷入口：进入赛前作战、查看证据完整性、打开复盘中心。

H5：

- 英雄区压缩为单列。
- KPI 单列或双列。
- 比赛队列转为卡片。

### 5.2 赛前作战

目的：按比赛聚合所有下注前信息。

内容：

- 比赛选择器和开赛倒计时。
- 官方核对状态：编号、玩法、让球、开售状态。
- 本届逐场表现摘要。
- 阵容、伤停、红黄牌、战意。
- 赔率快照与过期提醒。
- 舆情/天气/场地外部因素。
- 数据完整性检查。
- 推荐分析与风险摘要。

写操作如“保存分析草稿”“生成方案入库”必须登录。

### 5.3 证据中心

目的：统一承载原比赛、赔率、舆情、球队、球员模块。

桌面：使用二级导航或标签页。  
手机：使用分段控件或顶部横向滚动分类。

页面原则：

- 数据表格统一 `DataPanel` 外壳。
- 状态字段统一 `StatusBadge`。
- 原始 JSON 或 payload 使用折叠代码块，不默认撑开。
- 采集项审批按钮仅管理员可见；未登录显示只读说明。

### 5.4 决策与复盘

目的：呈现分析、投注方案、出票、赛果、复盘和规则沉淀闭环。

内容：

- 分析报告列表。
- 投注方案与票据。
- ROI / CLV / 盈亏指标。
- 赛后五层复盘：数学层、足球层、盘口层、大赛气质层、赔率价值层。
- 规则沉淀。

未登录可查看历史分析与复盘；新增、编辑、结算、保存规则必须登录。

### 5.5 管理后台

目的：管理员专用。

包含：

- JSON 审核中心。
- 扫描归档。
- 审核详情。
- 批准入库。
- 批量批准/驳回。
- 映射结果。
- 系统设置。

访问规则：

- 未登录访问 `/admin/*` 自动跳转 `/login?redirect=...`。
- 登录成功后回到原页面。
- 后端所有管理写接口强制鉴权。

## 6. 前端技术架构

### 6.1 组件分层

建议新增：

```text
client/src/components/app/
  AppShell.vue
  AppSidebar.vue
  AppTopbar.vue
  MobileTabbar.vue
  AdminGate.vue
  ReadonlyNotice.vue

client/src/components/ui/
  DataPanel.vue
  MetricCard.vue
  StatusBadge.vue
  RiskBadge.vue
  EmptyState.vue
  ActionToolbar.vue
  ResponsiveTable.vue

client/src/components/home/
  EventHero.vue
  MatchSpotlight.vue
  RiskQueue.vue
  StageEntryGrid.vue
```

### 6.2 状态管理

`auth` store 保留，但需要增强：

- 管理员身份。
- Basic Auth header。
- redirect 登录回跳。
- `canWrite` / `isAdmin` 计算属性。

### 6.3 API 客户端

- GET 查询方法默认不需要 authHeader。
- 写方法通过统一 helper 注入 Authorization。
- 401：提示登录并跳转。
- 403：提示权限不足。
- 不在每个页面手写鉴权逻辑，统一封装。

### 6.4 Element Plus 使用原则

保留 Element Plus 作为基础组件库，但通过全局 CSS 统一视觉：

- 卡片透明深色面板。
- 表格深色高对比样式。
- 按钮最小高度 44px。
- 表单 label 永远可见，不使用 placeholder 代替 label。
- Dialog/Drawer 在手机优先使用全屏或底部抽屉样式。

## 7. 后端权限与接口设计

### 7.1 Spring Security 调整

当前安全配置除健康检查和登录外全部要求认证。需要改为：

- `GET /api/health`：公开。
- `POST /api/auth/login`：公开。
- Swagger：保持公开，便于本地联调与接口核验。
- 业务查询型 `GET`：默认公开。
- `/api/import-*`、`/api/core-data/import-*` 等管理或写入端点：管理员。
- 所有 `POST / PUT / PATCH / DELETE`：默认管理员。

注意：JSON 审核中心的读取接口也建议管理员，因为原始归档详情属于管理上下文；若只展示“管理员待办数量”，应新增公开摘要接口，不复用审核详情接口。

### 7.2 新增公开聚合接口

为了首页避免多接口瀑布请求，第一版新增公开聚合接口：

```text
GET /api/public/overview
```

返回：

- 今日比赛摘要。
- 证据完整性计数。
- 赔率过期数量。
- 风险提醒数量。
- 分析/复盘统计。
- 管理待办数量，不含原始内容。

如果实现阶段发现已有等价公开聚合能力，可以复用既有服务逻辑，但对前端暴露的 DTO 仍保持本节定义，避免首页直接依赖多个业务接口。

## 8. 测试与验收

### 8.1 自动化验证

- `cd client && npm run build`
- `cd client && npm run test:run`
- `cd server && mvn test`

### 8.2 权限验收

- 未登录打开 `/` 成功。
- 未登录打开 `/workbench`、证据中心、决策复盘只读页面成功。
- 未登录访问 `/admin/import-review` 跳转登录。
- 未登录调用写接口返回 401/403。
- 登录后可进入 JSON 审核中心，并可批准入库。
- 登录后刷新页面权限状态符合当前存储策略；若不持久化密码，则需要重新登录。

### 8.3 响应式验收

必须检查：

- 375px 手机。
- 768px 平板。
- 1024px 小桌面。
- 1440px 桌面。

要求：

- 页面无整体横向滚动。
- 底部导航不遮挡内容。
- 表格在 H5 上转卡片或局部横向滚动。
- 点击目标 ≥44px。
- 登录、管理写操作、抽屉筛选在手机端可用。

### 8.4 可访问性验收

- 焦点环可见。
- 键盘可访问主导航和写操作。
- 表单 label 可见。
- 状态不只靠颜色表达。
- 深色模式文字对比度达标。
- `prefers-reduced-motion` 下减少动画。

## 9. 实施顺序建议

1. 建立设计 tokens、全局样式、AppShell、移动底部导航。
2. 调整路由和旧路由重定向。
3. 改 auth store 与 API 鉴权封装。
4. 调整后端 SecurityConfig 和权限测试。
5. 新建赛事总览首页。
6. 迁移赛前作战室。
7. 迁移证据中心五个子页。
8. 迁移决策与复盘。
9. 迁移管理后台 JSON 审核与设置。
10. 全量响应式、权限、构建与测试验收。

## 10. 非目标

- 不改变 `CLAUDE.md` 中的下注分析硬规则。
- 不删除 JSON 档案留存机制。
- 不把竞彩规则简化为普通体育新闻看板。
- 不为了视觉效果牺牲证据链、赔率、CLV、复盘可读性。
- 不引入大型 3D/WebGL 效果，避免性能和可访问性风险。

## 11. 自检结论

- 无未定内容。
- 范围聚焦在整站前端重设计与必要后端权限调整。
- 路由、权限、页面、H5、测试标准均已明确。
- 与项目规则不冲突：下注分析规则仍以 `CLAUDE.md` 为准。



