# 核心数据模型任务

- [x] 创建 `核心数据模型` OpenSpec change。
- [x] 新增核心业务表 Flyway 迁移。
- [x] 新增核心数据 JdbcTemplate 查询/写入模型与服务。
- [x] 新增 approved import item 正式入库服务。
- [x] 新增 ANALYSIS / ODDS / SOURCE / BETS 映射逻辑。
- [x] 新增核心数据 API：overview、import、mappings。
- [x] 新增后端测试覆盖权限、批准校验、幂等导入和四类 JSON 映射。
- [x] 新增前端 core data API helper 和轻量 UI 更新。
- [x] 运行 OpenSpec、后端、前端验证命令。
- [x] 完成子代理或 Codex 阶段审查并记录结果。
